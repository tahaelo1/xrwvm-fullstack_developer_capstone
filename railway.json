{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd server && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn djangoproj.wsgi",
    "restartPolicyType": "ON_FAILURE"
  }
}